<h2>Page for all projects</h2>

<p>List of all projects: </p>

<table>
  <tbody>
  <tr>
    <th>Project</th>
    <th>Company</th>
  </tr>
  <% @projects.each do |project| %>
      <tr>
        <td><%= link_to project, project %></td>
        <td><%= link_to project.company, project.company %></td>
      </tr>
  <% end %>
  </tbody>
</table>