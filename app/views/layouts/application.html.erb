<!DOCTYPE html>
<html>
<head>
  <title>Timetracker</title>
  <meta name="viewport" content="width=device-width", initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div id="container">
  <header>
    <%= link_to content_tag('canvas', 'Time Tracker', id: 'logocanvas'), root_url %>
    <nav>
      <ul>
        <% nav_items.each do |item| %>
            <%= display_nav_item( item[:displaytext], item[:controllername], item[:linkurl]) %>
        <% end %>
      </ul>
    </nav>
  </header>
  <section id="main">
    <article>
      <%= yield %>
    </article>
    <aside>
      <%= yield :aside %>
    </aside>
  </section>
  <footer>
    <p>Time Tracker is a <em>Sample Company</em> Application</p>
  </footer>
</div>

</body>
</html>
