<h2>New Company</h2>

<%= form_for @company do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.submit "Create Company" %>
  </div>
<% end %>

<% content_for :aside do %>
  <%= render 'sidebar' %>
<% end %>